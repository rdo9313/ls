#1
Logs [1,4,3] because the assignment is destructive.

#2
the variable 'greeting' is undefined within the current execution context.

#3
console.log(Math.sqrt(37))

#4
Math.max(1,6,3,2)
Math.max(-1,-6,-3,-2)
Math.max(2,2)

#5
Splits the string by space, reverses the resulting array, and then returns a new array with the character lengths 
per element (string) in the array.

#6
function logMatch(words, regex) {
  return words.filter(word => regex.test(word))
}

#7
Exception handling is dealing with errors in a manageable and predictable manner. 
Javascript uses try/catch to catch exceptions in the code.